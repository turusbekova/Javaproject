package com.java.class16;

public class WhileLoop {
    public static void main(String[] args) {
        // what are loops?
        // in programming pretty often you need to return the same set of
        // code multiple time and you need to accomplish that with
        // minimal code


        int activeUser = 0;
        activeUser++;
        System.out.println("Welcome User number " + activeUser);
        activeUser++;
        System.out.println("Welcome User number " + activeUser);
        activeUser++;
        System.out.println("Welcome User number " + activeUser);
        activeUser++;
        System.out.println("Welcome User number " + activeUser);
        activeUser++;
        System.out.println("Welcome User number " + activeUser);
        activeUser++;
        System.out.println("Welcome User number " + activeUser);
        activeUser++;
        System.out.println("Welcome User number " + activeUser);
        activeUser++;
        System.out.println("Welcome User number " + activeUser);
        activeUser++;
        System.out.println("Welcome User number " + activeUser);
        activeUser++;
        System.out.println("Welcome User number " + activeUser);

        System.out.println("__________________________________");
        // () - boolean expression
        // while boolean expression is true
        int activeU = 0;
        // if you are dealing with counters
        // you boolean expression are going to be
        // < comparing operator
        while (activeU < 10){
            System.out.println("Welcome User number " + activeU);
            activeU++;
        }










    }
}
